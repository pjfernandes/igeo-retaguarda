<h5 class="page-title"><i class="fas fa-edit text-primary"></i> Editar Campo</h5>

<%= simple_form_for( [current_user, @subject ], remote: true) do |f| %>

  <%= f.input :name, label: 'Nome', hint: 'Máximo de 30 caracteres' %>
  <%= f.input :teacher, label: 'Professor'%>
  <%= f.input :photo, label: 'Foto', as: :file %>
  <%= f.button :submit, "Editar", class:"btn btn-primary text-white mt-2 mb-2 pr-4 pl-4 mr-2" %>
  <%=link_to "Voltar", user_subjects_path(current_user), class:"btn btn-secondary mt-2 mb-2 pr-4 pl-4 mr-2"%>

<% end %>
